/** Duration (ms) the greeting animation plays before switching to idle. */
export const GREETING_DURATION_MS = 3000;

/** Distance (px) from bottom at which the chat auto-scrolls to new messages. */
export const SCROLL_STICK_THRESHOLD_PX = 120;
