/** Max characters shown in an auto-generated session title. */
export const SESSION_TITLE_MAX_LENGTH = 30;

/** Maximum number of sessions kept in localStorage. Oldest are pruned when exceeded. */
export const MAX_SESSIONS = 50;
